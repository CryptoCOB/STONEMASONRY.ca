# Database connection
DATABASE_URL=postgres://postgres:postgres@localhost:5432/stonemasonry

# Public ingest guard for quote/review capture
PUBLIC_API_KEY=replace-with-public-intake-key
DEFAULT_ACCOUNT_ID=replace-with-account-uuid

# Auth tokens
JWT_SECRET=replace-with-jwt-secret

# Optional server configuration
PORT=3001
